@{
    ViewData["Title"] = "Category";
}

@model IEnumerable<Shop.Models.Category>;

<style>
    section {
        min-height: 105vh;
    }

    .gapping {
        gap: 1rem;
    }

    .mls {
        margin-left: 40px;
    }
</style>

<div class="bs-dark">
    <section style="display:flex; flex-direction:column; gap: 1rem;">
        <div class="bs-dark-light1 p-4" style="display: flex; gap: 1rem;">
            <div class="container d-flex justify-content-center">
                <a class="head-font txt-light btn bs-blue" asp-action="Create"><i class="fas fa-plus mr-2"></i> Add new category</a>
            </div>
        </div>
        <div class="d-flex justify-content-center">
            @if (true)
            {
                <div class="mb-3 container d-flex pl-lg-5 pl-md-0">
                    <div class="row d-flex gapping mls">
                        @foreach (var item in Model)
                        {
                            <partial model="item" name="_devCategoryCard" />
                        }
                    </div>
                </div>
            }
            else
            {
                <h1 class="head-font txt-light">Add new device!</h1>
            }
        </div>
    </section>
</div>

@*<div>
    <div class="container p-0 mb-3">
        <a id="addbtn" asp-controller="Category" asp-action="Create" class="btn btn-primary pl-4 pr-4">Add</a>
    </div>
    @if (Model.Count() > 0)
    {
            <table class="table table-hover table-bordered">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Name</th>
                        <th scope="col">ShowOrder</th>
                        <th></th>
                    </tr>
                </thead >
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Name</td>
                            <td>@item.ShowOrder</td>
                            <td class="text-center">
                                <div class="btn-group">
                                    <a class="btn btn-warning text-light pl-4 pr-4" asp-route-Id="@item.Id" asp-controller="Category" asp-action="Edit">Edit</a>
                                    <a class="btn btn-danger text-light pl-4 pr-4" asp-route-Id="@item.Id" asp-controller="Category" asp-action="Delete">Delete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
    }
    else
    {
        <h1>Null</h1>
    }
</div>*@

